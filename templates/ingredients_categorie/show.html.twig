{% extends 'base.html.twig' %}

{% block title %}Catégorie d'ingrédients{% endblock %}

{% block body %}
    <h1>Catégorie d'ingrédients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredients_categorie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ingredients_categorie.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ingredients_categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_ingredients_categorie_index') }}">Retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('app_ingredients_categorie_edit', {'id': ingredients_categorie.id}) }}">Modifier</a>

    {{ include('ingredients_categorie/_delete_form.html.twig') }}
{% endblock %}
