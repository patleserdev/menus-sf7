<form method="post" action="{{ path('app_ingredients_by_recette_delete', {'id': ingredients_by_recette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredients_by_recette.id) }}">
    <button class="btn">Delete</button>
</form>
